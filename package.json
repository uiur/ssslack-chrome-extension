{
  "name": "ssslack",
  "version": "0.0.0",
  "dependencies": {
    "parse": "^1.4.2",
    "sanitize-html": "^1.6.1",
    "chrome-clip-copy": "uiureo/chrome-clip-copy"
  },
  "devDependencies": {
    "browserify": "^9.0.8",
    "domready": "^1.0.7",
    "standard": "^3.7.0",
    "watchify": "^3.2.1"
  },
  "scripts": {
    "build": "browserify src/content.js > dist/content.js",
    "watch": "watchify src/content.js -o dist/content.js -v &\\\n watchify src/background.js -o dist/background.js -v \\\n wait",
    "test": "standard src/**/*.js"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
